# AccessiBooks / MapAble – environment variables
# Copy to .env.local and fill in real values. Never commit .env.local or secrets.
# See env.dev for development template.

# Brand (optional): set to "accessibooks" for AccessiBooks; omit or leave unset for MapAble preview
# NEXT_PUBLIC_BRAND=accessibooks

# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/accessibooks

# NextAuth (required)
NEXTAUTH_SECRET=your_nextauth_secret_min_32_chars
# Local: http://localhost:3002  |  Production: https://mapable.com.au
NEXTAUTH_URL=http://localhost:3002

# Auth0 OAuth (optional)
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_ISSUER=https://your-tenant.auth0.com

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth (optional)
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Microsoft / Azure AD OAuth (optional)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=common

# Passport API bearer token (optional)
API_BEARER_TOKEN=

# Address lookup – Google Places (optional). Use GOOGLE_MAPS_API_KEY or GOOGLE_PLACES_API_KEY.
# Enable Places API (New) in Google Cloud. Used by GET /api/address/lookup.
# GOOGLE_MAPS_API_KEY=
# GOOGLE_PLACES_API_KEY=

# Address lookup – Amazon Location (optional). Requires place index and AWS credentials.
# AMAZON_PLACE_INDEX_NAME=

# ABR lookup (optional)
ABR_GUID=

# SendGrid (optional)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# AWS S3 (optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-southeast-2
S3_BUCKET=

# Google Cloud Storage (optional)
GCS_BUCKET=
GCP_PROJECT=
GOOGLE_APPLICATION_CREDENTIALS=

# Client
NEXT_PUBLIC_ORIGIN=http://localhost:3002

# Provider outlets: live NDIS list by default. Set to empty or "off" to use local public/data/provider-outlets.json.
# Optional: set a custom URL to override the default NDIS list-providers.json.
# NDIS_PROVIDER_LIST_URL=

# AccessiView (optional): video URL for 3D walkthrough page (YouTube, Vimeo, or direct .mp4)
NEXT_PUBLIC_ACCESSIVIEW_VIDEO_URL=
