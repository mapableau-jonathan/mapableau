// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("NON_POOLING_DATABASE_URL")
}

model Address {
  id       String  @id @default(uuid())
  addressString String
  formatted String? // full display string from Google, etc.
  street   String?
  suburb   String?
  city     String?
  state    String?
  postcode String?
  country  String  @default("Australia")

  latitude  Float?
  longitude Float?

  createdAt DateTime @default(now())

  providers              Provider[]
  providerOutlets        ProviderOutlet[]
  serviceLocations       ProviderServiceLocation[]
  outletServiceLocations ProviderOutletServiceLocation[]

  @@index([latitude, longitude])
  @@index([longitude])
}

model User {
  id               String            @id @default(cuid())
  name             String
  email            String            @unique
  worker           Worker?
  passwordHash     String
  createdAt        DateTime          @default(now())
  sessions         Session[]
  claimedProviders ClaimedProvider[]

  memberships       ProviderUserRole[]
  outletMemberships ProviderOutletUserRole[]
}

model Worker {
  id     String @id @default(uuid())
  userId String @unique
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  bio             String?
  qualifications  String?
  languages       WorkerLanguage[]
  availability    WorkerAvailability[]
  specialisations WorkerSpecialisation[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  providers   ProviderWorker[]
  outletLinks ProviderOutletWorker[]
}

model LanguageDefinition {
  id      String   @id @default(uuid())
  name    String
  workers WorkerLanguage[]
}

model WorkerLanguage {
  id         String   @id @default(uuid())
  workerId   String
  languageDefinitionId String

  worker   Worker   @relation(fields: [workerId], references: [id], onDelete: Cascade)
  languageDefinition LanguageDefinition @relation(fields: [languageDefinitionId], references: [id], onDelete: Cascade)

  @@unique([workerId, languageDefinitionId])
  @@index([languageDefinitionId])
}

model WorkerAvailability {
  id        String    @id @default(uuid())
  workerId  String
  dayOfWeek DayOfWeek
  startTime DateTime @db.Time
  endTime   DateTime @db.Time

  worker Worker @relation(fields: [workerId], references: [id], onDelete: Cascade)

  @@index([workerId])
}

model SpecialisationDefinition {
  id      String   @id @default(uuid())
  name    String @unique
  workers WorkerSpecialisation[]
  providerSpecialisations ProviderSpecialisation[]
  providerOutletSpecialisations ProviderOutletSpecialisation[]
}

model WorkerSpecialisation {
  id         String   @id @default(uuid())
  workerId String
  specialisationDefinitionId String

  worker Worker @relation(fields: [workerId], references: [id], onDelete: Cascade)
  specialisationDefinition SpecialisationDefinition @relation(fields: [specialisationDefinitionId], references: [id], onDelete: Cascade)

  @@unique([workerId, specialisationDefinitionId])
  @@index([specialisationDefinitionId])
}

model ClaimedProvider {
  id           String    @id @default(cuid())
  slug         String    @unique
  outletKey    String?   @unique
  userId       String
  user         User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  name         String
  phone        String?
  email        String?
  website      String?
  description  String?
  openingHours String?
  suburb       String?
  state        String?
  postcode     String?
  categories   String[]
  verifiedAt   DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model ProviderSpecialisation {
  id         String   @id @default(uuid())
  providerId String
  specialisationDefinitionId String

  provider Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  specialisationDefinition SpecialisationDefinition @relation(fields: [specialisationDefinitionId], references: [id], onDelete: Cascade)

  @@unique([providerId, specialisationDefinitionId])
  @@index([specialisationDefinitionId])
}

model ProviderOutletSpecialisation {
  id         String   @id @default(uuid())
  providerOutletId String
  specialisationDefinitionId String

  providerOutlet ProviderOutlet @relation(fields: [providerOutletId], references: [id], onDelete: Cascade)
  specialisationDefinition SpecialisationDefinition @relation(fields: [specialisationDefinitionId], references: [id], onDelete: Cascade)

  @@unique([providerOutletId, specialisationDefinitionId])
  @@index([specialisationDefinitionId])
}

model Provider {
  id          String  @id @default(uuid())
  name        String
  addressId   String
  logoUrl     String?
  description String?

  website String?
  email   String?
  phone   String?

  abn          String? @unique
  businessType String? // e.g. "Company", "Sole trader", "Partnership"

  ndisRegistered Boolean @default(false)
  ndisNumber     String?

  rating      Float? // average rating 0–5
  reviewCount Int    @default(0)

  serviceAreas String[] @default([])
// todo: look at what should happen when onDelete triggers
  address         Address                  @relation(fields: [addressId], references: [id], onDelete: SetNull)
  specialisations ProviderSpecialisation[]
  services        ProviderService[]
  locations       ProviderServiceLocation[]
  businessHours   ProviderBusinessHour[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  isActive Boolean @default(false)
  isVerified Boolean @default(false)
  search_vector Unsupported("tsvector")

  workers   ProviderWorker[]
  userRoles ProviderUserRole[]
  outlets   ProviderOutlet[]

  @@index([search_vector])
  @@index([addressId])
}

/// Physical or logical outlet under a provider (branch, site, listing).
model ProviderOutlet {
  id           String   @id @default(uuid())
  outletFingerprint         String
  name         String
  addressId    String?
  logoUrl     String?
  description  String?
  website      String?
  email        String?
  phone        String?
  ndisNumber   String?
  abn          String?
  businessType String?
  rating       Float?
  reviewCount  Int      @default(0)
  serviceAreas String[] @default([])

  providerId String
  provider   Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)

  address         Address?                        @relation(fields: [addressId], references: [id], onDelete: SetNull)
  specialisations ProviderOutletSpecialisation[]
  services        ProviderOutletService[]
  locations       ProviderOutletServiceLocation[]
  businessHours   ProviderOutletBusinessHour[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  isActive Boolean @default(false)
  isVerified Boolean @default(false)
  search_vector Unsupported("tsvector")

  workers   ProviderOutletWorker[]
  userRoles ProviderOutletUserRole[]

  @@unique([providerId, outletFingerprint])
  @@index([providerId])
  @@index([search_vector])
}

model ProviderUserRole {
  id         String   @id @default(uuid())
  userId     String
  providerId String
  assignedAt DateTime @default(now())

  role     ProviderRole
  user     User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  provider Provider     @relation(fields: [providerId], references: [id], onDelete: Cascade)

  @@unique([userId, providerId])
  @@index([providerId])
}

/// User role scoped to a specific provider outlet (mirrors ProviderUserRole).
model ProviderOutletUserRole {
  id               String   @id @default(uuid())
  userId           String
  providerOutletId String
  assignedAt       DateTime @default(now())

  role           ProviderRole
  user           User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  providerOutlet ProviderOutlet @relation(fields: [providerOutletId], references: [id], onDelete: Cascade)

  @@unique([userId, providerOutletId])
  @@index([providerOutletId])
}

model ServiceDefinition {
  id          String  @id @default(uuid())
  name        String
  description String?

  providers       ProviderService[]
  providerOutlets ProviderOutletService[]

  @@unique([name])
}

model ProviderService {
  id                  String   @id @default(uuid())
  serviceDefinitionId String
  providerId          String

  serviceDefinition ServiceDefinition @relation(fields: [serviceDefinitionId], references: [id], onDelete: Cascade)
  provider          Provider          @relation(fields: [providerId], references: [id], onDelete: Cascade)

  @@unique([serviceDefinitionId, providerId])
  @@index([providerId])
}

model ProviderOutletService {
  id                  String   @id @default(uuid())
  serviceDefinitionId String
  providerOutletId    String

  serviceDefinition ServiceDefinition @relation(fields: [serviceDefinitionId], references: [id], onDelete: Cascade)
  providerOutlet    ProviderOutlet    @relation(fields: [providerOutletId], references: [id], onDelete: Cascade)

  @@unique([serviceDefinitionId, providerOutletId])
  @@index([providerOutletId])
}

model ProviderServiceLocation {
  id         String   @id @default(uuid())
  addressId  String
  providerId String

  address  Address  @relation(fields: [addressId], references: [id], onDelete: Cascade)
  provider Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)

  @@unique([providerId, addressId])
  @@index([addressId])
}

model ProviderOutletServiceLocation {
  id               String   @id @default(uuid())
  addressId        String
  providerOutletId String

  address        Address        @relation(fields: [addressId], references: [id], onDelete: Cascade)
  providerOutlet ProviderOutlet @relation(fields: [providerOutletId], references: [id], onDelete: Cascade)

  @@unique([providerOutletId, addressId])
  @@index([addressId])
}

model ProviderBusinessHour {
  id         String    @id @default(uuid())
  dayOfWeek  DayOfWeek
  openTime   DateTime @db.Time
  closeTime  DateTime @db.Time
  providerId String

  provider Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)

  @@index([providerId])
}

model ProviderOutletBusinessHour {
  id               String    @id @default(uuid())
  dayOfWeek        DayOfWeek
  openTime         DateTime @db.Time
  closeTime        DateTime @db.Time
  providerOutletId String

  providerOutlet ProviderOutlet @relation(fields: [providerOutletId], references: [id], onDelete: Cascade)

  @@index([providerOutletId])
}

model ProviderWorker {
  id         String   @id @default(uuid())
  workerId   String
  providerId String

  worker   Worker   @relation(fields: [workerId], references: [id], onDelete: Cascade)
  provider Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)

  startDate DateTime?
  endDate   DateTime?

  @@unique([workerId, providerId])
  @@index([providerId])
}

/// Worker assignment to a specific provider outlet (mirrors WorkerProvider).
model ProviderOutletWorker {
  id               String   @id @default(uuid())
  workerId         String
  providerOutletId String

  worker         Worker         @relation(fields: [workerId], references: [id], onDelete: Cascade)
  providerOutlet ProviderOutlet @relation(fields: [providerOutletId], references: [id], onDelete: Cascade)

  startDate DateTime?
  endDate   DateTime?

  @@unique([workerId, providerOutletId])
  @@index([providerOutletId])
}

// todo: remove because we're using JWT
model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

enum ProviderRole {
  ADMIN
  MANAGER
  STAFF
}

enum DayOfWeek {
  MONDAY
  TUESDAY
  WEDNESDAY
  THURSDAY
  FRIDAY
  SATURDAY
  SUNDAY
}
